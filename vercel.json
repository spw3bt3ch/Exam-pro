{
  "version": 2,
  "installCommand": "python -m pip install --upgrade pip setuptools wheel && python -m pip install -r requirements.txt",
  "buildCommand": "",
  "routes": [
    { "src": "/$", "dest": "/api/app" },
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ]
}